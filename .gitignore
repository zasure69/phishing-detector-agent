__pycache__/
*.py[cod]
.env
.greennode.json
.agentbase/
.agentbase-state.json
*.credentials.json
.venv/
venv/
*.egg-info/
dist/
build/

# Local Claude Code config (machine-specific)
.claude/settings.local.json

# Real / uploaded emails may contain PII — keep them out of git.
# Synthetic demo samples under samples/eml/ are intentionally tracked.
/*.eml
/*.msg

# EICAR test-signature sample — regenerate with samples/eml/build_samples.py.
# Not committed so local AV doesn't quarantine it on clone.
samples/eml/04_*.eml


# macOS
.DS_Store
